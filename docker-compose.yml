services:
  db:
    image: postgres:16-alpine
    container_name: gestion-factures-db
    environment:
      POSTGRES_DB: gestion_factures
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
